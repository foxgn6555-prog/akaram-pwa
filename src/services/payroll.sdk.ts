/**
 * SDK: Payroll
 * TODO(v4): التنفيذ — اتبع النمط المرجعي في employees.sdk.ts:
 *   1) استورد { sdkGuard, supabase } من './client'
 *   2) صادّر كائن const payroll = { ... } بدوال موسومة الأنواع
 *   3) لا تلمس supabase خارج هذا الملف (قانون SDK — يفرضه ESLint)
 *   4) راجع migration الجدول المقابل في supabase/migrations
 */
export {}
