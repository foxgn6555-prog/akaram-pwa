import{useState,type ReactNode}from'react'
import{Boxes,PackagePlus,Search,TriangleAlert}from'lucide-react'
import{useMaintenanceCreateInventory,useMaintenanceInventory,useMaintenanceReceiveInventory}from'@features/vehicle-operations/hooks'
import type{MaintenanceInventoryItem}from'@sdk/vehicle-operations.sdk'
export default function MaintenanceInventoryPage(){const[search,setSearch]=useState(''),[receiving,setReceiving]=useState<MaintenanceInventoryItem|null>(null),[showCreate,setShowCreate]=useState(false);const q=useMaintenanceInventory(search);return <section dir="rtl" className="space-y-5"><header className="rounded-3xl bg-gradient-to-l from-slate-950 to-amber-700 p-6 text-white"><p className="flex items-center gap-2 text-xs"><Boxes size={17}/>رصيد حي وسجل حركات غير قابل للاستبدال</p><h1 className="mt-2 text-2xl font-black">مخزون قطع غيار الصيانة</h1></header><div className="flex flex-wrap gap-3 rounded-2xl border bg-white p-4"><label className="relative min-w-64 flex-1"><Search className="absolute right-3 top-3 text-slate-400" size={17}/><input aria-label="بحث المخزون" value={search} onChange={e=>setSearch(e.target.value)} className="h-11 w-full rounded-xl border pr-10" placeholder="رمز الصنف أو الاسم"/></label><button onClick={()=>setShowCreate(true)} className="flex h-11 items-center gap-2 rounded-xl bg-amber-700 px-5 font-black text-white"><PackagePlus size={17}/>صنف جديد</button></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{(q.data??[]).map(item=><article key={item.id} data-testid={`inventory-${item.id}`} className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex justify-between"><div><b className="rounded-lg bg-slate-900 px-2 py-1 text-xs text-white">{item.sku}</b><h2 className="mt-3 font-black">{item.item_name}</h2></div>{item.current_quantity<=item.low_stock_threshold&&<TriangleAlert className="text-amber-600"/>}</div><p className="mt-4 text-2xl font-black text-amber-800">{item.current_quantity} <small className="text-xs">{item.unit}</small></p><div className="mt-2 text-xs text-slate-500">متوسط الكلفة: {item.average_unit_cost.toLocaleString('ar-IQ')} · حد التنبيه: {item.low_stock_threshold}</div><button data-testid={`receive-${item.id}`} onClick={()=>setReceiving(item)} className="mt-4 h-10 w-full rounded-xl border border-amber-600 text-xs font-black text-amber-800">استلام كمية</button></article>)}</div>{showCreate&&<CreateDialog close={()=>setShowCreate(false)}/>} {receiving&&<ReceiveDialog item={receiving} close={()=>setReceiving(null)}/>}</section>}
function CreateDialog({close}:{close:()=>void}){const create=useMaintenanceCreateInventory();const[sku,setSku]=useState(''),[name,setName]=useState(''),[unit,setUnit]=useState('قطعة'),[threshold,setThreshold]=useState(0);return <Modal title="إضافة صنف مخزون" close={close}><form onSubmit={e=>{e.preventDefault();create.mutate({sku,name,unit,threshold},{onSuccess:close})}} className="space-y-3"><input required value={sku} onChange={e=>setSku(e.target.value)} className="h-11 w-full rounded-xl border px-3" placeholder="رمز الصنف SKU"/><input required minLength={2} value={name} onChange={e=>setName(e.target.value)} className="h-11 w-full rounded-xl border px-3" placeholder="اسم قطعة الغيار"/><div className="grid grid-cols-2 gap-2"><input value={unit} onChange={e=>setUnit(e.target.value)} className="h-11 rounded-xl border px-3"/><input type="number" min="0" value={threshold} onChange={e=>setThreshold(Number(e.target.value))} className="h-11 rounded-xl border px-3" placeholder="حد التنبيه"/></div><button className="h-11 w-full rounded-xl bg-amber-700 font-black text-white">حفظ الصنف</button></form></Modal>}
function ReceiveDialog({item,close}:{item:MaintenanceInventoryItem;close:()=>void}){const receive=useMaintenanceReceiveInventory();const[quantity,setQuantity]=useState(1),[cost,setCost]=useState(0),[notes,setNotes]=useState('');return <Modal title={`استلام ${item.item_name}`} close={close}><form onSubmit={e=>{e.preventDefault();receive.mutate({itemId:item.id,quantity,unitCost:cost,notes},{onSuccess:close})}} className="space-y-3"><input aria-label="الكمية المستلمة" type="number" min="0.001" step="0.001" value={quantity} onChange={e=>setQuantity(Number(e.target.value))} className="h-11 w-full rounded-xl border px-3"/><input aria-label="كلفة الوحدة" type="number" min="0" value={cost} onChange={e=>setCost(Number(e.target.value))} className="h-11 w-full rounded-xl border px-3"/><textarea value={notes} onChange={e=>setNotes(e.target.value)} className="w-full rounded-xl border p-3" placeholder="رقم الوصل أو ملاحظات الاستلام"/><button className="h-11 w-full rounded-xl bg-amber-700 font-black text-white">تأكيد الاستلام</button></form></Modal>}
function Modal({title,close,children}:{title:string;close:()=>void;children:ReactNode}){return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/60 p-4"><div className="w-full max-w-md rounded-3xl bg-white p-6"><div className="mb-4 flex justify-between"><h2 className="text-lg font-black">{title}</h2><button onClick={close}>إغلاق</button></div>{children}</div></div>}
