import SystemDashboard from '@components/dashboard/SystemDashboard'

/** لوحة البوابة التقنية — نفس لوحة النظام المشتركة (رسوم حقيقية) */
export default function ITDashboard() {
  return <SystemDashboard />
}
