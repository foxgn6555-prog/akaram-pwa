import{describe,expect,it,vi}from'vitest';import{complaintAnalyticsHtml,downloadComplaintAnalytics,printComplaintAnalytics,type ComplaintAnalytics}from'../../../src/features/complaints'
const data:ComplaintAnalytics={from:'2026-09-01',to:'2026-09-05',sector:null,summary:{total:60,approved:45,active:10,review:5,unassigned:0,withAfter:50},daily:[{day:'2026-09-05',total:60,approved:45,review:5,active:10}],statuses:[{status:'approved',count:45},{status:'in_progress',count:10}],sectors:[{sector:'karrada',count:30,approved:25},{sector:'zaafaraniya',count:30,approved:20}]}
describe('تقرير تحليلات الشكاوى القابل للطباعة',()=>{
 it('ينشئ تقريراً عربياً ذاتي التنسيق ويعقم النص',()=>{const html=complaintAnalyticsHtml(data,'monthly');expect(html).toContain('تقرير الشكاوى الشهري');expect(html).toContain('إجمالي الشكاوى');expect(html).toContain('<b>60</b>');expect(html).toContain('الكرادة');expect(html).toContain('@media print')})
 it('يحفظ التقرير كملف HTML مستقل',()=>{const click=vi.fn();vi.spyOn(document,'createElement').mockReturnValue({click}as unknown as HTMLElement);vi.stubGlobal('URL',{createObjectURL:vi.fn(()=> 'blob:test'),revokeObjectURL:vi.fn()});downloadComplaintAnalytics(data,'weekly');expect(click).toHaveBeenCalled();vi.restoreAllMocks()})
 it('يفتح نافذة الطباعة',()=>{const write=vi.fn(),print=vi.fn();vi.spyOn(window,'open').mockReturnValue({document:{write,close:vi.fn()},focus:vi.fn(),print}as unknown as Window);vi.useFakeTimers();printComplaintAnalytics(data,'annual');vi.runAllTimers();expect(write).toHaveBeenCalled();expect(print).toHaveBeenCalled();vi.useRealTimers();vi.restoreAllMocks()})
})
