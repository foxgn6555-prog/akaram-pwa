# سجل التغييرات

## v4.6.1 — 2026-08 (تحقق نهائي شامل + تحسينات الرئيسية والأرشيف)

### التحقق الشامل على قاعدة فارغة (PostgreSQL 17)
- **31/31 migration** من الصفر بلا أي خطأ
- **دخان v1**: 15 تأكيداً (جدول + RLS + سياسات + RPC + triggers + buckets)
- **دخان RPC public**: 11 تأكيداً (كل دالة عبر PostgREST)
- **دخان الجولة 2**: 15 تأكيداً (فروع · مصفوفة 8 · بوابات ديناميكية · بصمة merge · GPS)
- **عزل الجولة 6**: 22 تأكيداً (فروع · مصفوفة · can_i_see · بوابات · بصمة · GPS · سجلات)
- **ITDashboard**: 6/6 اختبار (الرسوم · المصادر الحقيقية · الروابط · لا بيانات وهمية)

### تحسينات إضافية
- الرئيسية: بطاقة متوسط قياسات + شريط معلومات النظام (PostgreSQL version · جداول · صفوف · أدنى/متوسط/أقصى latency)
- الأرشيف: بحث محلي في السجلات + حالة فراغ ذكية
- المستخدمون: زر تصدير CSV (BOM UTF-8 · افتح عربي في Excel)
- التحديثات: زر تحديث + فلتر مزود
- UserManagementHub: زر إنشاء سريع
- GPS: شريط المزودين المسجلين

## v4.6.0 — 2026-08 (الأرشيف الهندسي + الرئيسية الحية + الرسوم الحقيقية)

### منظومة الأرشيف (00029) — لا حذف فعلي
- أعمدة أرشفة (archived_at/by/reason) على 6 جداول: employees · departments · branches · biometric_devices · vehicles · dynamic_portals.
- RLS ذكي: المؤرشف يختفي من القراءة العادية ويظهر **حصرياً** لوحدة الأرشيف (IT).
- RPCs: archive_record (بالسبب) · archive_restore · archive_counts — كلها مدققة في audit_logs (قيد العملية وسّع بـ ARCHIVE/RESTORE في 00031).
- app_errors.archived: الأخطاء المغلقة تنتقل لأرشيف الأخطاء (تتبع تاريخي).

### قياسات الاتصال (00030)
- جدول connection_samples (24 ساعة) + RPCs: connection_sample · connection_history.
- نظافة تلقائية للقياسات القديمة.

### الصفحة الرئيسية — رسوم Recharts حقيقية 100%
- **سرعة الاتصال**: قياس فعلي (performance.now + REST) كل دقيقة → LineChart حي.
- **قاعدة البيانات**: أعمدة حجم الجداول من pg_stat الحقيقي.
- **أداء التطبيق**: Performance API (DNS · TTFB · DOM) حقيقي.
- توزيع الأدوار · تكاملات (نجاح/رفض) — من users.list وintegration_logs.

### وحدة الأرشيف الكاملة (صفحة)
- عدادات كل جدول · استعراض المؤرشف حسب الجدول · **استعادة بنقرة** · مبدأ الأرشيف موثق.
- SDK: archive.sdk (counts · listTable · restore · archive) + hooks.

### الاختبارات (163 ناجح)
- ITDashboard v2: الرسوم الحية + عناوين المصادر + لا بيانات وهمية.
- دخان الأرشيف (7 تأكيدات): أرشفة → إخفاء → أرشيف IT → استعادة → عدادات → قياسات.
- البنية: 8 وحدات IT (16 مسار).

## v4.5.0 — 2026-08 (جولات 2-5: البوابة التقنية الشاملة)

### جولة 2 — الأساس في قاعدة البيانات (00022-00028)
- 12 جدولاً جديداً: branches (+employees.branch_id) · role_page_permissions · user_page_overrides · dynamic_portals · portal_units · biometric_devices · biometric_pushes · gps_providers · vehicles · vehicle_positions · gps_events · integration_logs · system_settings — كلها RLS + فهارس + تدقيق.
- **دالة page_access الحاسمة** (أولوية: قفل فردي > إخفاء دور > منح دور > افتراضي البوابة > super_admin) + أغلفة public: can_i_see · page_access · biometric_ingest · gps_ingest.
- الأدوار الديناميكية `portal:{slug}` (فُتح القيد بأمان مع تحقق وجود البوابة).
- دخان الجولة 2: 15/15 ✅ على PostgreSQL 17 حقيقي (فروع · مصفوفة 8 سيناريوهات · بوابات ديناميكية · بصمة merge · GPS).

### جولة 3 — طبقة الوصول
- 4 SDKs: branches · permissions · portals (+UNIT_LIBRARY مكتبة وحدات جاهزة) · integrations.
- 4 مجموعات Query Keys + 12 hook (منها useLatestPositions بتحديث حي كل 30 ثانية).
- 11 اختبار SDK جديد.

### جولة 4 — صفحات البوابة التقنية
- **فروع الشركة**: إنشاء (Zod + uppercase) · تفعيل/تعطيل بنقرة.
- **مصفوفة الصلاحيات** ⚑: جدول تفاعلي صفحة×دور بنقرة دورية (افتراضي→منح→إخفاء) · القيود الفردية (قفل/فتح + سبب) · انعكاس فوري على الشريط الجانبي.
- **التكاملات**: أجهزة البصمة (حالة اتصال حية 🟢 + رابط ADMS جاهز للنسخ) · GPS (شاحنات + سرعة حية بالكم/س + إحداثيات + مزودون) — تحديث تلقائي 30 ثانية.
- **التحديثات والمراقبة**: سجل التكاملات المباشر + بطاقات صحة القاعدة.

### جولة 5 — المستقبلات الحية
- **Edge Function `adms-receiver`**: بروتوكول ZKTeco ADMS كامل (تسجيل options · نبض getrequest · دفع cdata→biometric_ingest · تأكيد devicecmd).
- **Edge Function `gps-receiver`**: صيغتان (Traccar JSON webhook + OsmAnd GET) · تحقق API key لكل مزود · تحويل speed knots→km/h.
- سكربتات: `npm run functions:deploy` · `npm run simulate -- --url ... --key ... --api-key ...`.
- محاكاة حية مثبتة على PostgreSQL: بصمة دفعة 4 أسطر → سجل حضور واحد بـ merge · GPS قُبل · غير المسجل مرفوض.

### الجودة الإجمالية
- **162 اختبار وحدة ناجح** · TSC/ESLint/Build نظيفة · دخان SQL شامل (15+15+29+10 تأكيداً).

## v4.4.2 — 2026-08 (إصلاح 404 الجذري: دوال RPC في المخطط العام)

- **السبب الجذري لـ 404**: دوال RPC كانت في مخطط `app` الداخلي بينما PostgREST (قناة `supabase.rpc()`) يكشف مخطط `public` فقط — حتى فحص القفل في الدخول كان يفشل بصمت.
- **الإصلاح (migration 00021)**: أغلفة `public.*` رفيعة تفوض لـ `app.*` (مصدر حقيقة واحد) لكل الدوال التسع: record_login_attempt · is_login_locked · login_lock_remaining_seconds · unlock_login · list_platform_users · set_user_role · db_stats · db_overview · db_table_details — مع Grants دقيقة (anon للدخول، authenticated للإدارة).
- **اختبارات جديدة تمنع العودة**:
  - `supabase/tests/rpc_public_test.sql` — 10 تأكيدات: كل دالة تُنادى عبر `public.` بالضبط كما يفعل PostgREST (استجابة + رفض أمني + حماية ذات + وصول فعلي للبيانات).
  - الدخان يفحص الآن **الأغلفة الثمانية في public** — أي دالة ناقصة = فشل بناء صريح «سينتج 404 في التطبيق!».
- auth.sdk: فشل فحص القفل يُسجل تحذيراً واضحاً (لا صمت أمني).
- تحقق كامل على PostgreSQL 17: 21/21 migration · SMOKE OK · 29/29 عزل · 10/10 RPC.

## v4.4.1 — 2026-08 (إصلاحات تخطيط الواجهة — من لقطات المستخدم)

- **الهيكل المكسور**: الحاوية الخارجية لم تكن `flex` — الشريط الجانبي (ارتفاع الشاشة) كان يُكدّس فوق عمود المحتوى فيظهر الهيدر أسفل الصفحة. الآن: `flex` حقيقي — الشريط بجانب المحتوى والهيدر أعلاه دائماً.
- **الشريط يختفي عند التمرير**: أصبح `lg:sticky lg:top-0` — يبقى مثبتاً طوال الصفحة.
- **الانهيار كان يحذف الشريط كلياً**: الآن يضيّقه إلى `w-20` (أيقونات فقط) كما صُمم.
- **درج الموبايل كان يُفتح تلقائياً**: يبدأ مغلقاً ويُفتح من زر الهيدر.
- **Placeholder ممتد بشكل مشوه**: عرض محصور `max-w-3xl` متمركز.
- OfflineBanner نُقل داخل عمود المحتوى (لم يعد يعترض تخطيط Flex).

## v4.4.0 — 2026-08 (البوابة التقنية مكتملة المحتوى — تركيز حصري)

### وحدة الرئيسية (جديدة — فهرس البوابة)
- لوحة حية: المستخدمون · أصحاب الأدوار · حجم القاعدة · الأخطاء المفتوحة (كلها RPC/استعلامات حقيقية).
- توزيع الأدوار بأشرطة + تحذير أصحاب الحسابات بلا أدوار.
- أحدث الأخطاء المفتوحة (5) + تنبيه «موظفون بلا حساب» مع انتقال مباشر للإنشاء.
- روابط سريعة: إنشاء مستخدم · الهيكل التنظيمي · سجل الأخطاء.

### وحدة قاعدة البيانات — استكمال
- **migration 00020**: `db_table_details(p_table)` — أعمدة الجدول وأنواعه وحجمه (RPC آمنة بفحص دور + قائمة سماح، مُختبرة: 14 عموداً لـ employees).
- **صفحة تفاصيل الجدول** (`/it/database/tables/:tableName`): أعمدة/أنواع/إلزامية + الصفوف والحجم — الجداول في النظرة العامة أصبحت قابلة للنقر.
- سجل الأخطاء: صفوف **قابلة للتوسيع** (stack + user-agent + context JSON).

### وحدة إدارة المستخدمين — إثراء
- شريط إحصائيات: الإجمالي · أصحاب الأدوار الإدارية · بلا أدوار (تحذيري).

### تحسينات عرض
- توحيد الأرقام اللاتينية (`ar-IQ-u-nu-latn`) في كل المنصة — أوضح للوحات البيانات.

### الاختبارات (+14 = 100 إجمالاً)
لوحة IT (5) · تفاصيل الجدول (3) · توسيع الأخطاء (1) · بنية IT بوحداتها الثلاث (4) · مبدّل البوابات في UserMenu (4) · انحدار تغطية المسارات.
20/20 migration على PostgreSQL 17 حقيقي + SMOKE OK.

## v4.3.0 — 2026-08 (إصلاح جذر: كل الصفحات تعمل بعد الدخول + أوامر الترمينال + الهيكل التنظيمي)

### إصلاح حرج
- **«الضغط على أي صفحة يعيدني لتسجيل الدخول»**: البوابات الأخرى كانت `children: []` فأي رابط وحدة يسقط في wildcard `*` → redirect /login. الحل الجذري: **مولّد مسارات** (`unit-routes.tsx`) يبني Route فعلياً لكل وحدة في كل بوابة (صفحات جاهزة أو Placeholder احترافي) + اختبار انحدار يمنع عودة الثغرة + جذر ذكي `/` وwildcard آمن.

### البوابة التقنية — اكتملت وظيفياً
- **صفحة الهيكل التنظيمي** (جديدة): عرض الأقسام + إنشاء قسم رئيسي/فرعي → جدول departments مباشرة (migration 00019 وسّع سياسة الأقسام لتشمل it_admin — مُختبرة على PostgreSQL حقيقي).
- إنشاء الحسابات والأدوار يعمل من التطبيق (Edge Function admin-users) ومن الترمينال.

### أوامر الترمينال الجديدة (Windows/Linux/macOS)
- `npm run role:set -- <email> <role> [--revoke]` — منح/سحب دور + إنشاء سجل الموظف تلقائياً.
- `npm run user:create -- <email> <pass> "<الاسم>" <role> [رقم]` — مستخدم كامل جاهز للدخول.
- `npm run functions:deploy` — نشر Edge Function لإنشاء الحسابات من داخل التطبيق.
- `scripts/db-push.mjs` أصبح Node (يعمل على Windows — bash أُزيل).

### الاختبارات (+14 = 86 إجمالاً)
- **انحدار تغطية المسارات لكل البوابات** (6) · بنية IT v2 (4) · الهيكل التنظيمي (5) · departments.sdk (1).
- migration 00019 مُتحقق منها على PostgreSQL 17 محلي + SMOKE OK + إثبات إنشاء قسم بـ it_admin.

## v4.2.0 — 2026-08 (البوابة التقنية: وحدتا إدارة المستخدمين وقاعدة البيانات)

### وحدة إدارة المستخدمين (3 صفحات)
- **المستخدمون**: قائمة بكل مستخدمي المنصة (RPC آمنة) + بحث مؤجل + شارات أدوار عربية + ربط لسجل الموظف.
- **إنشاء مستخدم**: نموذج كامل (بريد/كلمة مرور بسياسة صارمة/دور/ربط موظف وقسم) → **Edge Function `admin-users`** (service_role داخل الدالة فقط + تحقق دور المتصل + تراجع نظيف عند فشل تعيين الدور).
- **إدارة الأدوار**: منح/سحب فوري عبر RPC مع منع تعديل الذات + تأكيد خاص لمنح الإدارة العليا + تدقيق إلزامي في audit_logs.

### وحدة قاعدة البيانات (صفحتان)
- **نظرة عامة**: حجم القاعدة + عدد الجداول + إجمالي الصفوف + جدول أحجام الجداول مع مسح تسلسلي/فهرسي (RPC: db_overview/db_stats).
- **أخطاء التطبيق**: جدول `app_errors` جديد (00017) يستقبل أخطاء العميل تلقائياً عبر logger → main.tsx (محدود المعدل 20/جلسة + إزالة تكرار) — عرض + مرشحات + حلّ/إعادة فتح بتحديث متفائل.

### أساس
- migration 00018: 4 RPCs جديدة بفحص دور صارم (anon يرى فراغاً/يُرفض — مختبر).
- Sidebar: دعم الصفحات الفرعية (children) — تظهر عند نشاط الوحدة.
- صفحة 403 حقيقية + ربط مسارات IT في الموجّه مع index redirect.

### الاختبارات (+21)
- مخطط إنشاء المستخدم (8) · SDK الوحدة التقنية (10) · القائمة (6) · الإنشاء (4) · الأخطاء (6) · الشريط الجانبي IT (4) · **اتساق البنية routes↔sidebar (5)** · تكامل RPC على الخادم (6) · E2E IT (3).
- smoke_schema مطوّل: 15 جدولاً + دوال 00016+00018 كلها · CI أضافت بوابة production build.

## v4.1.0 — 2026-08 (هوية جزيرة الأكرام + أمن الدخول + الشريط الجانبي)

### الهوية
- الاسم الرسمي: **جزيرة الأكرام** (المجلد الرئيسي بالعربية) — ألوان مستخرجة من الشعار: أزرق #005F8D · أصفر #FFCA00.
- أصول مولّدة: icon-192/512 · maskable · apple-touch · favicon.ico · logo.png (PIL من الشعار الأصلي).
- CSP في index.html · المنصة عربية 100% (i18n عربي فقط مع بنية جاهزة لـ EN).

### أمن تسجيل الدخول (3 طبقات)
- **migration 00016**: login_attempts + RPCs (إقفال 5/15د · مسح تلقائي · unlock إداري) — الجدول بلا أي سياسة RLS.
- **login-guard.ts**: حارس متصفح بعد تنازلي — طبقة UX فوق حكم الخادم.
- **auth.sdk**: فحص القفل قبل المحاولة + تسجيل النجاح/الفشل على الخادم.
- صفحة Login جديدة كاملة: Zod + إظهار/إخفاء كلمة المرور + رسالة موحدة لا تسرّب + تعطيل الحقول أثناء القفل.

### الشريط الجانبي الاحترافي
- PORTAL_UNITS: كل بوابة تعرض وحداتها فقط (6/6/5/5/4/6 وحدة) من مكان واحد (config).
- Sidebar: RTL كامل · انهيار دسكتوب · درج موبايل بـ backdrop · حالة نشطة aria-current · أيقونات SVG مضمّنة (لا CDN).
- Header + UserMenu + NotificationBell · AppShell موحد لكل البوابات السبع.

### الاختبارات
- LoginPage (7) · login-guard (6) · Sidebar (4) · E2E login (4) · تكامل أمن الدخول على الخادم (4).
- **db-smoke.yml**: كل PR يطبق المهاجرات على قاعدة فارغة ويفحص: 14 جدولاً + RLS كامل + ≥50 سياسة + triggers + buckets.

## v4.0.0 — 2026-08 (إعادة بناء الهيكلية عن v3.0)

### إصلاحات معمارية
- **إصدارات حديثة**: React 19 · RRv7 · Tailwind v4 (CSS-first: حذف tailwind.config.ts + postcss.config.js) · Vite 7 · Zod 4 · ESLint 9 Flat Config (بدل .eslintrc.json المهجورة).
- **Triggers داخل migrations**: حذف مجلد `supabase/triggers/` المنفصل (كان لن يُطبَّق تلقائياً أبداً).
- **RLS مع كل domain**: تفكيك 00011_rls_policies الضخم — كل سياسة بجانب جدولها.
- **`index.html`**: كان مفقوداً كلياً عن v3 رغم أنه شرط Vite.
- **حذف `public/manifest.json`**: توليد المانيفست من pwa.config (كان تعارضاً في v3).
- **حذف `.env.local` من الشجرة**: ملف بيئة لا يُرفع للمستودع أبداً.
- **عيب اكتمال**: features (payroll/attendance/assets/audit) بلا migrations في v3 — أضيفت 00009/00010/00013 + audit.keys.ts + audit/index.ts المفقودة.
- **فصل departments.sdk عن employees.sdk** (كانا سيكسران حد 300 سطر).
- **إضافة مصفوفة انتقالات الحالة** المطابقة للـ DB trigger (كانت v3 تعكس الدوال دون انتقالات).
- **طبقات جديدة**: `lib/offline` (سياسة تعارض كاملة — ADR 006) · `lib/monitoring` (Sentry اختياري — ADR 007).
- **أدوات إنتاجية**: scaffold-feature.sh · check-env.ts بـ Zod · vite-tsconfig-paths (لا تكرار aliases) · coverage gate 70%.

### إضافات
- 7 ADRs موثقة · docs/offline-policy.md · SECURITY.md · CONTRIBUTING.md · CHANGELOG.md
- أكملت البوابات السبع قوقعة موحدة (AppShell pattern) مع ثيمات CSS لكل بوابة.

## v3.0.0 — 2026-07
- الوثيقة النهائية الأصلية (Feature-Driven · MakerKit RLS · TanStack Ship · Workbox).
